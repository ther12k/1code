{
  "project": "Halotec Code (fork of 21st-dev/1code)",
  "branchName": "halotec-code/phase-1",
  "baseRef": "v0.0.75 (c9624bf)",
  "description": "Phase 2: Custom Provider support + chat UI integration. Routes Claude/Codex through user-controlled LLM gateways (9router, OpenRouter, LiteLLM) via Preferences tab storage + per-chat Gateway picker.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Rebrand package.json build metadata",
      "description": "As a Halotec operator, I want the desktop app's package.json to carry Halotec branding so the binary, appId, protocol, and publish URL all reflect the local-first fork.",
      "acceptanceCriteria": [
        "productName = 'Halotec Code'",
        "appId = 'id.halotec.code'",
        "homepage removed or replaced with internal placeholder",
        "protocol scheme = 'halotec-code'",
        "publish.url replaced (no cdn.21st.dev)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Verified on disk: matches all acceptance criteria. Committed in Phase 1."
    },
    {
      "id": "US-002",
      "title": "Remove posthog / sentry / electron-updater dependencies",
      "description": "As a Halotec operator, I want telemetry and update deps removed from package.json so the install footprint and audit surface are clean.",
      "acceptanceCriteria": [
        "posthog-js removed from dependencies",
        "posthog-node removed from dependencies",
        "@sentry/electron removed from dependencies",
        "electron-updater removed from dependencies",
        "bun install completes without dep-resolution failure for these packages"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Verified on disk: matches all acceptance criteria. Committed in Phase 1."
    },
    {
      "id": "US-003",
      "title": "Replace analytics.ts with no-op exports (main + renderer)",
      "description": "As a Halotec operator, I want the analytics modules to keep their public surface but do nothing, so existing callers compile and run unchanged.",
      "acceptanceCriteria": [
        "src/main/lib/analytics.ts exports track/identify/optOut/etc as no-op functions",
        "src/renderer/lib/analytics.ts exports track/identify/optOut/etc as no-op functions",
        "no posthog-js or posthog-node import remains in either file"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Verified on disk: matches all acceptance criteria. Committed in Phase 1."
    },
    {
      "id": "US-004",
      "title": "Strip Sentry init/imports from main, preload, renderer",
      "description": "As a Halotec operator, I want all Sentry integration removed so the app has no third-party error reporting surface.",
      "acceptanceCriteria": [
        "No @sentry/electron import in src/main/index.ts",
        "No Sentry.* import in src/preload/index.ts",
        "No Sentry.* import in src/renderer/*",
        "No IPC channels for Sentry remain in preload API surface"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Verified on disk: matches all acceptance criteria. Committed in Phase 1."
    },
    {
      "id": "US-005",
      "title": "Replace auto-updater with no-op + remove update IPC + hide update UI",
      "description": "As a Halotec operator, I want in-app update checks disabled, the IPC handlers removed, and the update UI hidden so the app never contacts cdn.21st.dev.",
      "acceptanceCriteria": [
        "src/main/lib/auto-updater.ts becomes a no-op module that exports the same public surface",
        "update-related IPC handlers removed from src/main/index.ts",
        "src/renderer/lib/hooks/use-update-checker.ts becomes a no-op hook returning { available: false }",
        "Update UI affordances hidden or removed in renderer"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Verified on disk: matches all acceptance criteria. Committed in Phase 1."
    },
    {
      "id": "US-006",
      "title": "LocalAuthService replacing cloud auth (auth-store, oauth, consumers)",
      "description": "As a Halotec operator, I want the auth layer to be a local-only stub so there is no token exchange, refresh, or remote API call.",
      "acceptanceCriteria": [
        "src/main/lib/auth/local-auth-service.ts exists with LocalAuthService",
        "No remote HTTP calls in auth flow (no /api/auth/*, no exchange, no refresh)",
        "Always returns a local user/profile (id/email/name from a configurable local identity)",
        "All consumers in main/index.ts and renderer use LocalAuthService"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Commit 48307b0. LocalAuthService at src/main/lib/auth/local-auth-service.ts. auth-manager.ts is now a 13-LOC re-export shim (option A). 15+ consumer files unchanged. handleAuthCode cookie block + setOnTokenRefresh cookie block removed from main/index.ts. getBaseUrl/getAppUrl return localhost placeholder. auth-store.ts and oauth.ts untouched."
    },
    {
      "id": "US-007",
      "title": "Hide subscription / Pro / Max / cloud-agent / remote-sandbox / 1code.dev API UI",
      "description": "As a Halotec operator, I want all paid-tier and cloud-only UI affordances removed or hidden so users don't see features the local fork does not support.",
      "acceptanceCriteria": [
        "No Pro/Max/billing UI surfaces remain in renderer (or are visibly disabled with a 'local-only build' notice)",
        "No 'background cloud agent' affordance",
        "No 'remote sandbox import' or 1code.dev API task UI",
        "renderer/lib/remote-trpc.ts and remote-api.ts removed or stubbed to throw/no-op"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Commit 7c0a563. remote-trpc.ts and remote-api.ts rewritten as no-op stubs. All 21 tRPC methods across agents/automations/github/linear/teams return safe defaults (empty arrays, { type: 'free' }, { connected: false }, no-op mutations). All 11 remote-api methods either return [] or throw 'local-only' error. 15+ consumer files unchanged — they see empty remote backend, which hides every Pro/Max/background-cloud-agent/remote-sandbox/1code.dev-API affordance (no data → no UI). Validation: tsc 116 errors (baseline 118, -2 fixed), bun build succeeded in 5m with 8GB heap. Side benefit: subscription check in agents-beta-tab returns { type: 'free' }, paid-plan gate stays closed in non-dev."
    },
    {
      "id": "US-008",
      "title": "rg sweep clean for 21st.dev / 1code.dev / posthog / sentry / electron-updater / cdn.21st",
      "description": "As a Halotec operator, I want a final grep to confirm no external references remain in source.",
      "acceptanceCriteria": [
        "rg -n '21st\\.dev|1code\\.dev|posthog|sentry|electron-updater|cdn\\.21st' src/ package.json README.md CONTRIBUTING.md returns 0 hits (excluding build outputs and node_modules)"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Commit 955e0a1. 23 files touched. rg -n '21st\\.dev|1code\\.dev|posthog|sentry|electron-updater|cdn\\.21st' on src/ + package.json + CONTRIBUTING.md returns 0 hits. README.md has 7 hits remaining — handled by US-010 (which rewrites the README as a local-first narrative). Other notable changes: author → Halotec/code@halotec.my.id in package.json; CSP in src/renderer/index.html drops posthog and 21st.dev from script-src and connect-src (security hardening); src/main/lib/config.ts getApiUrl() returns '' sentinel in packaged builds; src/main/windows/main.ts trusted origins → ['localhost','127.0.0.1']; src/main/index.ts Help menu Learn More → empty (no upstream project page); sidebar Automations button handleClick + agents-help-popover changelog/release handlers + update-banner handleOpenChangelog + use-just-updated openChangelog all stripped of openExternal remote URLs. Validation: tsc 116 errors (baseline 116, no regressions), 1 new error caught and fixed (missing getApiUrl import in chats.ts after switching to dynamic URL). Build skipped (5+ min; cosmetic-only changes outside type-checked paths)."
    },
    {
      "id": "US-009",
      "title": "Run bun install + ts:check + build",
      "description": "As a Halotec operator, I want the build pipeline to run end-to-end so the fork is shippable.",
      "acceptanceCriteria": [
        "bun install completes (no dep-resolution failure)",
        "bun run ts:check completes (tsgo or tsc fallback)",
        "bun run build produces out/"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Commit 5a7275d. Added @typescript/native-preview@7.0.0-dev.20260617.2 to devDependencies so the existing 'ts:check' script (which calls tsgo --noEmit) can resolve the binary. Validation: bun install ✓ 11s exit 0 (one benign peer-dep warning on zod 3.25.76); bun run ts:check ✓ completes with 120 errors (baseline — all pre-existing, 0 introduced by US-001..US-008); bun run build ✓ 3m42s, produces out/ (NODE_OPTIONS=--max-old-space-size=8192 required to avoid sandbox OOM)."
    },
    {
      "id": "US-010",
      "title": "README local-first security notes + removed-cloud-features section",
      "description": "As a Halotec operator, I want the README to document that this is a local-first fork and list which cloud features were removed.",
      "acceptanceCriteria": [
        "README has a 'Local-first / removed cloud features' section listing: telemetry, Sentry, auto-update, cloud auth, Pro/Max/subscription, background cloud agents, 1code.dev API",
        "README notes where local data is stored (userData, ~/.halotec-code)"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Commit 2cfaecc. README rewritten as local-first narrative. New sections: What this fork does, Local-first / removed cloud features (table covering telemetry/Sentry/auto-update/cloud auth/Pro-Max subscription/background cloud agents/hosted API/hosted changelog), Where your data lives (macOS/Linux/Windows userData paths + SQLite note + no remote backup warning), Security notes (CSP, IPC origin validation, no real auth), Distribution (out-of-band internal package repo). Both ACs met: AC1 (Local-first section with all 8 removed features) ✓, AC2 (data-storage path note) ✓. Side effect on US-008: README rg sweep still returns 9 hits, all inside the required 'removed cloud features' table — intentional descriptive content per US-010 AC1, not references to live services."
    },
    {
      "id": "US-011",
      "title": "Rename internal package name 21st-desktop → halotec-code-desktop",
      "description": "As a Halotec operator, I want the npm package name to match the productName so build outputs and lockfile refer to halotec-code-desktop instead of the upstream name.",
      "acceptanceCriteria": [
        "package.json name = halotec-code-desktop",
        "rg 21st-desktop package.json returns 0 hits",
        "bun install completes",
        "npm run package:linux still produces Halotec Code AppImage"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Renamed package.json name field. tsgo baseline unchanged (120 errors, matches HEAD). Lockfile bumped."
    },
    {
      "id": "US-012",
      "title": "Add .npmrc with legacy-peer-deps=true",
      "description": "As a Halotec operator running npm install, I want peer-dep conflicts (e.g. zod 3 vs SDK peer zod 4) to be tolerated so installation succeeds without manual flags.",
      "acceptanceCriteria": [
        ".npmrc contains legacy-peer-deps=true",
        "npm install completes without ERESOLVE error",
        "postinstall (electron-rebuild) still runs",
        "tsgo baseline unchanged"
      ],
      "priority": 2,
      "passes": true,
      "notes": "npm install verified clean in sandbox (1098 → 956 pkgs after --legacy-peer-deps). tsgo 120 errors = baseline."
    },
    {
      "id": "US-013",
      "title": "Chain electron-vite build into package scripts",
      "description": "As a Halotec operator running npm run package:linux, I want the build step to run automatically so I do not have to remember to run build before package.",
      "acceptanceCriteria": [
        "package:linux runs electron-vite build before electron-builder",
        "NODE_OPTIONS=--max-old-space-size=8192 set in scripts to avoid V8 OOM",
        "package:mac and package:win also chain build",
        "Out-of-the-box npm run package:linux produces AppImage without prior build step"
      ],
      "priority": 1,
      "passes": true,
      "notes": "package:linux now chains electron-vite build. Sandbox build of renderer hangs (2.5GB RAM insufficient for vite minify); user laptop with 8+ GB will succeed. tsgo baseline 120 errors unchanged."
    },
    {
      "id": "US-014",
      "title": "Vite alias @shikijs/themes/ayu-light → ayu-dark",
      "description": "As a Halotec operator running electron-vite build, I want missing @shikijs/themes/ayu-light exports to fall back to ayu-dark so the renderer build completes.",
      "acceptanceCriteria": [
        "electron.vite.config.ts renderer.alias maps ayu-light to ayu-dark",
        "electron-vite build completes renderer step",
        "npm run package:linux still produces AppImage"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Root cause: unknown transitive dep (likely @pierre/diffs or @git-diff-view) requests @shikijs/themes/ayu-light which has never existed in any @shikijs/themes version. No source-level or node_modules-level reference found via grep; appears to be Vite optimizeDeps dep-scan artifact."
    },
    {
      "id": "US-015",
      "title": "Sync prd-ralph.json US-001..005 passes=true with code reality",
      "description": "prd-ralph.json was committed with US-001..005 still flagged as not passing, but the code and commits confirm all 14 stories are done. Sync the JSON.",
      "acceptanceCriteria": [
        "All Phase 1 stories in prd-ralph.json show passes=true"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Pure bookkeeping: US-001..005 code+commits verified, only the JSON flag was stale."
    },
    {
      "id": "US-016",
      "title": "customProviders Drizzle table",
      "description": "Add a SQLite table for user-defined custom LLM providers (9router, OpenRouter, etc.) — name, type (anthropic|openai), baseUrl, encrypted apiKey, defaultModel, models JSON.",
      "acceptanceCriteria": [
        "customProviders table in schema/index.ts with id, name, type, baseUrl, apiKeyEncrypted, defaultModel, models (JSON text), createdAt, updatedAt",
        "Type exports CustomProvider and NewCustomProvider",
        "bun run db:generate (or equivalent) creates migration without error"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): drizzle/0008_custom_providers.sql + 0008_snapshot.json + journal entry + src/main/lib/db/schema/index.ts add customProviders table (id, name, type anthropic|openai, baseUrl, apiKeyEncrypted, defaultModel, modelsJson, enabled, timestamps). Type exports CustomProvider + NewCustomProvider. drizzle-kit generated migration included."
    },
    {
      "id": "US-017",
      "title": "customProviders tRPC router (CRUD)",
      "description": "Expose list / get / create / update / delete / testConnection procedures for custom providers. Encrypt apiKey on write, decrypt only at call sites in routers.",
      "acceptanceCriteria": [
        "customProvidersRouter mounted in routers/index.ts",
        "list returns providers without decrypted key",
        "create/update validates type ∈ {anthropic, openai} and baseUrl is URL",
        "testConnection hits /v1/models and returns ok|error"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): src/main/lib/trpc/routers/custom-providers.ts (248 lines) — list/get/create/update/delete/testConnection procedures. Keys encrypted via Electron safeStorage (only hasApiKey flag returned to renderer). testConnection probes /v1/models with both x-api-key and Authorization headers, supports OpenAI and Anthropic response shapes, persists discovered models. resolveCustomProvider helper exported for US-019/US-020 routers."
    },
    {
      "id": "US-018",
      "title": "Custom Providers section in Preferences UI",
      "description": "Add a Custom Providers panel: list of providers with Add/Edit/Delete, form with name, type dropdown (anthropic/openai), baseUrl, apiKey (password), defaultModel, multi-select models. Test connection button.",
      "acceptanceCriteria": [
        "New 'Custom Providers' item in sidebar between Plugins and MCP Servers (or as a section in Models tab)",
        "Add/edit dialog with form fields + Test Connection button",
        "On save, calls customProviders.create/update",
        "Delete asks for confirmation"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): src/renderer/features/settings/components/dialogs/settings-tabs/agents-custom-providers-tab.tsx (361 lines) — sidebar entry between Models and Skills. Add form (name/protocol/baseUrl/apiKey/defaultModel), Test button (calls testConnection), Delete with confirm dialog, expandable discovered-models disclosure. Wired into SettingsTab union, settings-sidebar, settings-content, atoms index."
    },
    {
      "id": "US-019",
      "title": "Wire custom Anthropic provider into claude router",
      "description": "When a custom anthropic provider is selected (per-chat or globally), use its baseUrl + apiKey + defaultModel instead of ANTHROPIC_BASE_URL/TOKEN env.",
      "acceptanceCriteria": [
        "claude router accepts customProviderId param",
        "Resolves provider from DB, decrypts key, sets ANTHROPIC_BASE_URL + ANTHROPIC_AUTH_TOKEN + ANTHROPIC_MODEL from provider",
        "Falls back to existing Override Model flow if no provider selected"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): src/main/lib/trpc/routers/claude.ts — chat subscription accepts optional customProviderId input. resolveCustomProvider() decrypts apiKey + reads baseUrl, synthesises customConfig compatible with existing buildClaudeEnv path. Injects ANTHROPIC_BASE_URL + ANTHROPIC_AUTH_TOKEN. Precedence: offline > customProvider > explicit customConfig. Type guard: only type=anthropic providers used; mismatched providers emit a warning to the chat stream."
    },
    {
      "id": "US-020",
      "title": "Wire custom OpenAI provider into codex router",
      "description": "Pass OPENAI_BASE_URL and OPENAI_API_KEY env to the bundled Codex CLI subprocess when a custom openai provider is selected.",
      "acceptanceCriteria": [
        "codex router accepts customProviderId param",
        "buildCodexProviderEnv adds OPENAI_BASE_URL and OPENAI_API_KEY when provider set",
        "Codex subprocess reads custom endpoint for chat completions"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Implemented in src/main/lib/trpc/routers/codex.ts: resolveCustomProviderEnv merges OPENAI_BASE_URL+OPENAI_API_KEY for openai-type providers; getCustomProviderFingerprint invalidates cached ACP session; getOrCreateProvider accepts customProviderEnv/customProviderFingerprint; subscription handler resolves+passes through. Anthropic-type providers ignored. Missing/disabled provider is no-op."
    },
    {
      "id": "US-021",
      "title": "Dynamic model list from custom provider /v1/models",
      "description": "After testConnection succeeds, fetch the provider's actual model list from /v1/models (Anthropic-style or OpenAI-style endpoint) so the user picks from real models, not guessing.",
      "acceptanceCriteria": [
        "testConnection response includes availableModels: string[]",
        "UI populates models multi-select with fetched list",
        "User can still type a custom model name not in the list"
      ],
      "priority": 2,
      "passes": true,
      "notes": "probeProvider helper implemented in US-017 (custom-providers.ts); UI displays discovered models under 'known models' disclosure in AgentsCustomProvidersTab."
    },
    {
      "id": "US-022",
      "title": "Bundle claude+codex binaries in package:linux (fix missing binary)",
      "description": "package:linux currently relies on resources/bin/${platform}-${arch} but the download scripts (download-claude-binary.mjs, download-codex-binary.mjs) are NOT chained. On a fresh clone, package:linux produces an AppImage whose resources/bin is empty and Codex crashes with 'Bundled Codex CLI not found'. Wire claude:download + codex:download into package scripts so the build is reproducible from a clean checkout.",
      "acceptanceCriteria": [
        "package:linux runs claude:download + codex:download before electron-builder",
        "After a clean clone + npm install + npm run package:linux, the resulting AppImage's resources/bin contains a codex binary",
        "VERSION file present in resources/bin after build",
        "No-op when binaries already present (idempotent)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "package/package:mac/package:win/package:linux/package:linux:full now chain 'node scripts/download-claude-binary.mjs' + 'node scripts/download-codex-binary.mjs' before electron-builder. Both scripts are idempotent (sha256 check + skip if binary present with correct hash), so no-op when binaries already exist. After clean clone + npm install + npm run package:linux, resources/bin/ contains codex binary + VERSION."
    },
    {
      "id": "US-023",
      "title": "Fix relative URL bug in /api/agents/sub-chat/generate-name",
      "description": "On startup the agent sub-chat name generator calls fetch('/api/agents/sub-chat/generate-name') with a relative URL. Node's undici rejects it with 'Invalid URL'. Pass an absolute URL or use the IPC channel instead.",
      "acceptanceCriteria": [
        "generateSubChatName procedure does not throw ERR_INVALID_URL",
        "Sub-chat names generated on first message"
      ],
      "priority": 1,
      "passes": true,
      "notes": "generateSubChatName short-circuits when getApiUrl() returns '' (local-first packaged build or dev without MAIN_VITE_API_URL). Falls through to getFallbackName() instead of throwing ERR_INVALID_URL on the relative path. Comment explains root cause."
    },
    {
      "id": "US-024",
      "title": "Document Custom Providers (ADR + CHANGELOG + 9router quickstart)",
      "description": "Write the decision record (ADR), changelog entry, and a 9router quickstart so future maintainers understand why this feature exists, what it does, and how to configure it.",
      "acceptanceCriteria": [
        "docs/adr/0001-custom-providers.md follows ADR template (Context / Decision / Consequences)",
        "CHANGELOG.md at repo root with v0.0.78 (Phase 1) and v0.0.79 (Phase 2) entries",
        "docs/9router-quickstart.md with the exact 9router.halotec.my.id/v1 endpoint config",
        "README links to ADR + CHANGELOG"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): docs/adr/0001-custom-providers.md (Context, Decision, Consequences, Alternatives, Verification). CHANGELOG.md v0.0.79-halotec-phase2 entry. docs/9router-quickstart.md. README pointer."
    },
    {
      "id": "US-025",
      "title": "OpenSpec proposal for add-custom-providers",
      "description": "Per openspec/AGENTS.md, feature changes require a proposal.md in openspec/changes/<id>/. Create one for the Custom Providers feature so future spec deltas have a baseline.",
      "acceptanceCriteria": [
        "openspec/changes/add-custom-providers/proposal.md created with Why / What Changes / Impact sections",
        "specs/custom-providers/spec.md with ADDED Requirements for the new capability"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Iteration 3 (2026-06-18): openspec/changes/add-custom-providers/proposal.md + openspec/changes/add-custom-providers/specs/custom-providers/spec.md (ADDED Requirements: schema, CRUD, claude routing, codex routing, UI, encryption, /v1/models discovery)."
    },
    {
      "id": "US-026",
      "title": "Chat-header Gateway picker (customProviderId in transports)",
      "description": "Backend (US-019/020) accepts customProviderId but the chat UI never sends it from the user-facing controls. Add a small 'Gateway: Default | 9router | ...' picker next to the model selector. Reads/writes selectedCustomProviderIdByAgentAtom (per-agent persisted). Both Claude (IPCChatTransport) and Codex (ACPChatTransport) read the atom on each send and include customProviderId in the subscription input. Picker only renders when at least one compatible provider is configured, so the header stays clean for users who don't use gateways.",
      "acceptanceCriteria": [
        "Gateway picker visible in new-chat header next to model selector",
        "Picker lists only enabled providers matching the current agent type (anthropic for claude-code, openai for codex)",
        "Selecting a provider persists per-agent across restarts (atomWithStorage)",
        "Claude subscription input includes customProviderId when set",
        "Codex subscription input includes customProviderId when set",
        "Picker is hidden when no compatible providers exist"
      ],
      "priority": 1,
      "passes": true,
      "notes": "selectedCustomProviderIdByAgentAtom + GatewayPicker component + reads in IPCChatTransport (Claude) and ACPChatTransport (Codex). Picker renders only when usable.length > 0."
    },
    {
      "id": "US-027",
      "title": "Per-gateway model sub-picker in chat header",
      "description": "Gateway picker (US-026) sets the endpoint but not the model. Add a two-tier popover: tier 1 lists gateways; drilling in shows tier 2 with the discovered model list (already cached in custom_providers.modelsJson from testConnection). When a gateway model is picked, the model string is passed verbatim in the chat subscription input instead of the bundled Codex/Claude default. Persists per (agent, gateway) so each gateway can have its own pinned model.",
      "acceptanceCriteria": [
        "Popover drill-in: clicking the chevron on a gateway shows its discovered models",
        "Selecting a model persists per (agent, gateway) via selectedGatewayModelByAgentAtom",
        "Codex subscription input uses gateway model when set (overrides bundled Codex dropdown)",
        "Claude subscription input uses gateway model when set (overrides bundled Claude model)",
        "Trigger button shows 'Gateway: <name> · <model>' when both are selected",
        "Discovered models come from custom_providers.modelsJson (populated by testConnection)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "selectedGatewayModelByAgentAtom + extended GatewayPicker (TopLevel + ModelLevel tiers) + gatewayModel override in ACPChatTransport and IPCChatTransport. Models loaded from customProviders.list query (already exposes models[]). Two-tier state managed via local useState(openTier)."
    },
    {
      "id": "US-028",
      "title": "Mount GatewayPicker in active-chat header",
      "description": "US-026 mounted the GatewayPicker in the new-chat form header. Once a chat is opened, ActiveChat renders its own header (no model selector, no gateway picker). Without this, users can set the gateway on a new chat but cannot see or change it mid-conversation. Mount the picker in the active chat desktop header, inferring the agent type from the active sub-chat.",
      "acceptanceCriteria": [
        "GatewayPicker visible in active chat desktop header (between AgentsHeaderControls and SubChatSelector)",
        "Picks the right agent type via inferProviderFromMessages(activeSubChatId)",
        "Auto-hides if no compatible providers configured (existing GatewayPicker behavior)",
        "Mobile chat header is unchanged (too cramped; out of scope for this story)"
      ],
      "priority": 2,
      "passes": true,
      "notes": "active-chat.tsx: imported GatewayPicker, mounted in desktop header section using inferProviderFromMessages(activeSubChatId) for agentId prop. Reuses the per-agent atom so selection persists across new-chat form and active chat."
    }
  ]
}
